align-content → có nhiều dòng flex
align-self → căn chính bản thân trong flex cha, di vs flexbox
text-align → căn text trong block thường, không phải flex center chuẩn


- export const searchSongs =  async( req, res)=>{
    export: cho phép file khác import
    async: xử lí bất đồng bộ
    req (request): chứa mọi thứ user gửi lên 
    res (response): Trả dl về FE

- Trong JSX, mọi thứ là JavaScript phải đặt trong {}
